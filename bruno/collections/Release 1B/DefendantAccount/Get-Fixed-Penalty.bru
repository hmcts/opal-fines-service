meta {
  name: Get-Fixed-Penalty
  type: http
  seq: 4
}

get {
  url: {{baseURL}}/defendant-accounts/{{defendantAccount_idWithImpositions}}/fixed-penalty
  body: none
  auth: bearer
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
