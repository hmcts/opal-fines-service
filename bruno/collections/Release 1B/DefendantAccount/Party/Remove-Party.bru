meta {
  name: Remove-Party
  type: http
  seq: 7
}

delete {
  url: {{baseURL}}/defendant-accounts/{{defendantAccountId}}/defendant-account-parties/{{defendantAccountPartyId}}
  body: json
  auth: inherit
}

headers {
  Business-Unit-Id: 77
  If-Match: "{{accountVersion}}"
}

body:json {
  {
    "party_details": {
      "party_id": "{{partyId}}"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
