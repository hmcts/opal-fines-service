openapi: 3.1.1
info:
  title: Opal Fines Service - Notes
  version: 1.0.0
  description: APIs for managing notes.
paths:
  /notes/add:
    post:
      tags:
        - notes
      summary: Add a note to an entity
      operationId: addNote
      parameters:
        - in: header
          name: Business-Unit-Id
          description: Business unit identifier
          required: true
          schema:
            type: integer
            format: int16
        - in: header
          name: If-Match
          description: Version of the defendant account. DB Mapping - defendant_accounts.version
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddNoteRequest'
      responses:
        "201":
          description: Note created
          content:
            application/json:
              schema:
                type: string
        "400":
          description: Bad request
          content:
            application/json+problem:
              schema:
                $ref: './common.yaml#/components/schemas/ProblemDetail'
        "401":
          description: Unauthorized
          content:
            application/json+problem:
              schema:
                $ref: './common.yaml#/components/schemas/ProblemDetail'
        "403":
          description: Forbidden
          content:
            application/json+problem:
              schema:
                $ref: './common.yaml#/components/schemas/ProblemDetail'
        "500":
          description: Internal server error
          content:
            application/json+problem:
              schema:
                $ref: './common.yaml#/components/schemas/ProblemDetail'
        "503":
          description: Service unavailable
          content:
            application/json+problem:
              schema:
                $ref: './common.yaml#/components/schemas/ProblemDetail'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    AddNoteRequest:
      additionalProperties: false
      type: object
      x-json-include: NON_NULL
      x-implements:
        - uk.gov.hmcts.opal.dto.ToJsonString
      required:
        - activity_note
      properties:
        activity_note:
          $ref: '#/components/schemas/ActivityNote'
    ActivityNote:
      title: Note to add
      type: object
      additionalProperties: false
      properties:
        record_type:
          type: string
          enum:
            - defendant_accounts
            - creditor_accounts
            - suspense_accounts
            - report_instances
        record_id:
          type: string
        note_text:
          type: string
        note_type:
          type: string
          const: AA
      required:
        - record_type
        - record_id
        - note_text
        - note_type
